/**
 * @alchyx/react — the Alchyx component library.
 *
 *   import "@alchyx/tokens/css";        // design-token variables (once, at root)
 *   import "@alchyx/react/styles.css";  // component styles (or bundle per-import)
 *   import { AlchyxProvider, Button } from "@alchyx/react";
 */

// Foundations — headless behavior primitives + theme
export * from "./lib";
export * from "./theme";

// Components (kept in alphabetical order)
export * from "./components/button";
